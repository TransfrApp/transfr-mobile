import {
    Dimensions
} from 'react-native';

const {width, height} = Dimensions.get('window');

const appStyles = {
topNavIconRight: {
    height: 20,
    width: 20,
    marginRight: 15
}
}

export default appStyles;