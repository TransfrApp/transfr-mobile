import {
    Dimensions
} from 'react-native';

const {width, height} = Dimensions.get('window');

const appStyles = {
topNavIcon: {
    height: 20,
    width: 20,
    padding: 10
}
}

export default appStyles;